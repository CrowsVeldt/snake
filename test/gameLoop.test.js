/* eslint-env jest */

import gameLoop from '../src/gameLoop'

// TODO: create mock state and dispatch to test with

describe('gameLoop', () => {
  test('should be a function', () => {
    expect(typeof gameLoop).toEqual('function')
  })
})
